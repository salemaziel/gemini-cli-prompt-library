prompt = """
# SSL/TLS Optimization

Optimize the SSL/TLS configuration for:

{{args}}

## Configuration Checklist

### 1. Protocol Support
- Enable **TLS 1.2** and **TLS 1.3**.
- Disable SSLv3, TLS 1.0, and TLS 1.1 (Depreciated/Insecure).

### 2. Cipher Suites
- Prioritize strong, forward-secrecy ciphers (ECDHE-ECDSA-AES256-GCM-SHA384).
- Disable weak ciphers (RC4, DES).

### 3. Features
- **HSTS (HTTP Strict Transport Security):** Force HTTPS. Include `preload` directive.
- **OCSP Stapling:** Improve handshake speed.
- **HTTP/2 & HTTP/3 (QUIC):** Enable for performance.

## Output
Provide the Nginx/Apache configuration block or Cloudflare settings advice.
"""
